<h3>Add a Comment:</h3>
<%= form_for @song.comments.build do |f| %>
  <%= f.label "Content" %>
  <%= f.text_field :content %>
  <%= f.collection_select :user, User.all, :id, :name, include_blank: 'Choose a User' %>
  <%= f.hidden_field :song_id %>

  
  <%= f.submit %>
<% end %>
