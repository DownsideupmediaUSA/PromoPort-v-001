<h3><%= @artist.artist_name%></h3>

<br>

<ul>

    <% @artist.songs.each do |song|%>
    <li>
        <a href="<%= song_path(song) %>"><%= song.title %></a>
      </li>
      <%end%>

      

  </ul>
