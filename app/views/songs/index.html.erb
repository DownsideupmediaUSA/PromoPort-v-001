<h5>All Tracks</h5>


<ul>

    <% @songs.each do |song|%>
    <li>
        <a href="<%= song_path(song) %>"><%= song.title %></a>
      </li>
      <%end%>

  </ul>

Go to <%= link_to "All Artists", artists_path%><br>
